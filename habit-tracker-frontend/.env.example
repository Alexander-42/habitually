# Optional. Leave unset to call the API on the same origin (relative /api),
# which is how the production build is served by the backend.
# In dev, Vite proxies /api to the backend, so this is usually not needed.
# Set it only to point the app at a backend on a different origin.
# VITE_API_URL=http://localhost:4000
